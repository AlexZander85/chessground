name: Finalize multiple-premoves core

'on':
  push:
    branches:
      - agent/multiple-premoves-web-core-finalize

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: bash .github/finalize-multiple-premoves.sh
